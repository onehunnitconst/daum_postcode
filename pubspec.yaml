name: daum_postcode
description: Daum Postcode Service for Flutter
version: 1.0.0
homepage: https://github.com/onehunnitconst/daum_postcode

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  freezed: ^2.4.1
  build_runner: ^2.3.2
  json_serializable: ^6.6.2

flutter:

